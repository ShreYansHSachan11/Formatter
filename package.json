{
  "name": "exam-paper-formatter",
  "version": "1.0.0",
  "private": true,
  "description": "Formats school examination papers into a clean two-page A4 Word document.",
  "scripts": {
    "test": "node tests/check-integrity.js && node tests/check-header.js && node tests/check-ui.js && node tests/check-numbering.js",
    "build-samples": "node tests/build-docx.js tests/fixtures/loose-spacing.txt"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "jsdom": "^30.0.1"
  }
}
